server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Flutter Web uses path-based routing; fallback to index.html on refresh.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
